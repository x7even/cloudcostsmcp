apiVersion: v1
kind: Service
metadata:
  name: opencloudcosts
  namespace: opencloudcosts
  labels:
    app: opencloudcosts
spec:
  selector:
    app: opencloudcosts
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
