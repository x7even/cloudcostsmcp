apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opencloudcosts

resources:
  - namespace.yaml
  - secret.yaml        # copy from secret.yaml.example and populate before applying
  - deployment.yaml
  - service.yaml
  # - ingress.yaml   # uncomment to expose externally
